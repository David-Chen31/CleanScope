<Application x:Class="CleanScope.App.Wpf.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:CleanScope.App.Wpf.Converters"
             xmlns:vm="clr-namespace:CleanScope.App.Wpf.ViewModels"
             xmlns:views="clr-namespace:CleanScope.App.Wpf.Views">
    <Application.Resources>
        <ResourceDictionary>

            <!-- 主题色板 (E: 浅色默认; ThemeManager 运行时替换 [0] 即切换深浅, DynamicResource 消费方实时重着色) -->
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/Light.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <!-- 转换器 -->
            <conv:RiskToBrushConverter x:Key="RiskToBrush"/>
            <conv:BoolToVisibilityConverter x:Key="BoolToVis"/>
            <conv:NotEmptyToVisibilityConverter x:Key="NotEmptyToVis"/>
            <conv:FactToBrushConverter x:Key="FactToBrush"/>
            <conv:BucketToBrushConverter x:Key="BucketToBrush"/>
            <conv:FractionToWidthConverter x:Key="FractionToWidth"/>
            <conv:EqualsMultiConverter x:Key="Eq"/>
            <conv:UsageToBrushConverter x:Key="UsageToBrush"/>
            <conv:AiDotBrushConverter x:Key="AiDotBrush"/>

            <!-- VM → View 映射 (Shell 的 ContentControl 依此渲染) -->
            <DataTemplate DataType="{x:Type vm:HomeViewModel}"><views:HomeView/></DataTemplate>
            <DataTemplate DataType="{x:Type vm:SpaceMapViewModel}"><views:SpaceMapView/></DataTemplate>
            <DataTemplate DataType="{x:Type vm:ExplorerViewModel}"><views:ExplorerView/></DataTemplate>
            <DataTemplate DataType="{x:Type vm:SpaceBySoftwareViewModel}"><views:SpaceBySoftwareView/></DataTemplate>
            <DataTemplate DataType="{x:Type vm:FileDetailViewModel}"><views:FileDetailView/></DataTemplate>
            <DataTemplate DataType="{x:Type vm:ReportViewModel}"><views:ReportView/></DataTemplate>
            <DataTemplate DataType="{x:Type vm:AiSettingsViewModel}"><views:AiSettingsView/></DataTemplate>

            <!-- ============ 视觉系统 v2 (色板见 Themes/Light.xaml + Dark.xaml) ============ -->

            <!-- 阴影 (卡片分层, 去扁平; 收敛强度让卡片更"嵌在同一面板"而非浮起的子窗口) -->
            <DropShadowEffect x:Key="CardShadow" BlurRadius="10" ShadowDepth="1"
                              Direction="270" Opacity="0.06" Color="#27324A"/>
            <DropShadowEffect x:Key="PopShadow" BlurRadius="24" ShadowDepth="6"
                              Direction="270" Opacity="0.22" Color="#1A2230"/>

            <!-- ===== 现代细滚动条 (统一观感; 替换 Win 默认粗灰条, 消除"窗口里套窗口") ===== -->
            <Style x:Key="ScrollThumb" TargetType="Thumb">
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Setter Property="IsTabStop" Value="False"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Thumb">
                            <Border x:Name="t" CornerRadius="4" Background="#C7CFDB" Margin="2"/>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="t" Property="Background" Value="#A7B2C2"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="ScrollPageButton" TargetType="RepeatButton">
                <Setter Property="OverridesDefaultStyle" Value="True"/>
                <Setter Property="IsTabStop" Value="False"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RepeatButton"><Border Background="Transparent"/></ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="ScrollBar">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Width" Value="11"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ScrollBar">
                            <Grid Background="Transparent">
                                <Track x:Name="PART_Track" IsDirectionReversed="True">
                                    <Track.Thumb><Thumb Style="{StaticResource ScrollThumb}"/></Track.Thumb>
                                    <Track.IncreaseRepeatButton>
                                        <RepeatButton Command="ScrollBar.PageDownCommand" Style="{StaticResource ScrollPageButton}"/>
                                    </Track.IncreaseRepeatButton>
                                    <Track.DecreaseRepeatButton>
                                        <RepeatButton Command="ScrollBar.PageUpCommand" Style="{StaticResource ScrollPageButton}"/>
                                    </Track.DecreaseRepeatButton>
                                </Track>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="Orientation" Value="Horizontal">
                                    <Setter Property="Width" Value="Auto"/>
                                    <Setter Property="Height" Value="11"/>
                                    <Setter TargetName="PART_Track" Property="Orientation" Value="Horizontal"/>
                                    <Setter TargetName="PART_Track" Property="IsDirectionReversed" Value="False"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <!-- 大容器获得键盘焦点时不再显示虚线焦点框 (消除"点首页周围出现虚线"的怪异感) -->
            <Style TargetType="ScrollViewer">
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            </Style>

            <!-- 卡片 -->
            <Style x:Key="Cards" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource Card}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Line}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="Padding" Value="20"/>
                <Setter Property="Margin" Value="0,0,0,14"/>
                <Setter Property="Effect" Value="{StaticResource CardShadow}"/>
            </Style>

            <!-- 主按钮 (hover/press/focus/disabled) -->
            <Style x:Key="Primary" TargetType="Button">
                <Setter Property="Background" Value="{DynamicResource Accent}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="16,9"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="8"
                                    Padding="{TemplateBinding Padding}" BorderThickness="0"
                                    RenderTransformOrigin="0.5,0.5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="{DynamicResource AccentHover}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="{DynamicResource AccentPress}"/>
                                    <Setter TargetName="b" Property="RenderTransform">
                                        <Setter.Value><ScaleTransform ScaleX="0.98" ScaleY="0.98"/></Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="b" Property="BorderThickness" Value="2"/>
                                    <Setter TargetName="b" Property="BorderBrush" Value="#99FFFFFF"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="b" Property="Background" Value="#A9BBD8"/>
                                    <Setter TargetName="b" Property="Opacity" Value="0.7"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 次要按钮 (hover 浅强调底 + 蓝边) -->
            <Style x:Key="Ghost" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource Accent}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource Line}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="14,8"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="{DynamicResource AccentSoft}"/>
                                    <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="#D8E6FE"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="b" Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 现代输入框 (焦点蓝边 + 辉光) -->
            <Style x:Key="Field" TargetType="TextBox">
                <Setter Property="Padding" Value="10,8"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
                <Setter Property="CaretBrush" Value="{DynamicResource Accent}"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="b" Background="{DynamicResource Input}" BorderBrush="{DynamicResource Line}"
                                    BorderThickness="1.5" CornerRadius="8">
                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource InputBorderHover}"/>
                                </Trigger>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                    <Setter TargetName="b" Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect BlurRadius="6" ShadowDepth="0" Color="#2563EB" Opacity="0.25"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 密码框 (同款外观) -->
            <Style x:Key="FieldPassword" TargetType="PasswordBox">
                <Setter Property="Padding" Value="10,8"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
                <Setter Property="CaretBrush" Value="{DynamicResource Accent}"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="PasswordBox">
                            <Border x:Name="b" Background="{DynamicResource Input}" BorderBrush="{DynamicResource Line}"
                                    BorderThickness="1.5" CornerRadius="8">
                                <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource InputBorderHover}"/>
                                </Trigger>
                                <Trigger Property="IsKeyboardFocused" Value="True">
                                    <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                    <Setter TargetName="b" Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect BlurRadius="6" ShadowDepth="0" Color="#2563EB" Opacity="0.25"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ToggleSwitch (滑块开关, 替代"启用"复选框) -->
            <Style x:Key="Toggle" TargetType="ToggleButton">
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="track" Width="42" Height="24" CornerRadius="12" Background="{DynamicResource Track}">
                                <Ellipse x:Name="thumb" Width="18" Height="18" Fill="White"
                                         HorizontalAlignment="Left" Margin="3,0,0,0">
                                    <Ellipse.Effect><DropShadowEffect BlurRadius="3" ShadowDepth="1" Opacity="0.3"/></Ellipse.Effect>
                                </Ellipse>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="track" Property="Background" Value="{DynamicResource Accent}"/>
                                    <Setter TargetName="thumb" Property="HorizontalAlignment" Value="Right"/>
                                    <Setter TargetName="thumb" Property="Margin" Value="0,0,3,0"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="track" Property="Opacity" Value="0.88"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 文字层级 -->
            <Style x:Key="Display" TargetType="TextBlock">
                <Setter Property="FontSize" Value="28"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
            </Style>
            <Style x:Key="H1" TargetType="TextBlock">
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
                <Setter Property="Margin" Value="0,0,0,8"/>
            </Style>
            <Style x:Key="H2" TargetType="TextBlock">
                <Setter Property="FontSize" Value="15"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
                <Setter Property="Margin" Value="0,0,0,6"/>
            </Style>
            <Style x:Key="Sub" TargetType="TextBlock">
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="Foreground" Value="{DynamicResource Muted}"/>
                <Setter Property="TextWrapping" Value="Wrap"/>
            </Style>
            <Style x:Key="Caption" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Foreground" Value="{DynamicResource Faint}"/>
            </Style>

            <!-- 通用 chip 容器 (圆角标签) -->
            <Style x:Key="Chip" TargetType="Border">
                <Setter Property="CornerRadius" Value="6"/>
                <Setter Property="Padding" Value="7,2"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>

            <!-- ===== 原生控件主题化 (消除"拼装感") ===== -->

            <!-- 复选框 -->
            <Style TargetType="CheckBox">
                <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="CheckBox">
                            <StackPanel Orientation="Horizontal" Background="Transparent">
                                <Border x:Name="box" Width="18" Height="18" CornerRadius="5" Background="{DynamicResource Input}"
                                        BorderBrush="{DynamicResource Line}" BorderThickness="1.5" VerticalAlignment="Center">
                                    <TextBlock x:Name="check" Text="✓" FontSize="12" Foreground="White" FontWeight="Bold"
                                               HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                                </Border>
                                <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="box" Property="Background" Value="{DynamicResource Accent}"/>
                                    <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                    <Setter TargetName="check" Property="Visibility" Value="Visible"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="box" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 单选钮 -->
            <Style TargetType="RadioButton">
                <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Margin" Value="0,3"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <StackPanel Orientation="Horizontal" Background="Transparent">
                                <Border x:Name="o" Width="18" Height="18" CornerRadius="9" Background="{DynamicResource Input}"
                                        BorderBrush="{DynamicResource Line}" BorderThickness="1.5" VerticalAlignment="Center">
                                    <Ellipse x:Name="dot" Width="8" Height="8" Fill="{DynamicResource Accent}" Visibility="Collapsed"/>
                                </Border>
                                <ContentPresenter Margin="8,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="o" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                    <Setter TargetName="dot" Property="Visibility" Value="Visible"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="o" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 下拉框 -->
            <Style TargetType="ComboBox">
                <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
                <Setter Property="Height" Value="38"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <ToggleButton Focusable="False" ClickMode="Press"
                                              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border x:Name="bd" Background="{DynamicResource Input}" BorderBrush="{DynamicResource Line}"
                                                    BorderThickness="1.5" CornerRadius="8">
                                                <Path x:Name="ar" Data="M0,0 L4,4 L8,0 Z" Fill="{DynamicResource Muted}"
                                                      HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bd" Property="BorderBrush" Value="{DynamicResource InputBorderHover}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  Margin="12,0,30,0" VerticalAlignment="Center" IsHitTestVisible="False"/>
                                <Popup x:Name="PART_Popup" Placement="Bottom" Focusable="False"
                                       IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" PopupAnimation="Slide">
                                    <Border Background="{DynamicResource Card}" BorderBrush="{DynamicResource Line}" BorderThickness="1" CornerRadius="8"
                                            Margin="0,4,0,0" Effect="{StaticResource PopShadow}"
                                            MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                        <ScrollViewer MaxHeight="260">
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style TargetType="ComboBoxItem">
                <Setter Property="Padding" Value="12,8"/>
                <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="b" Background="Transparent" Padding="{TemplateBinding Padding}" CornerRadius="6" Margin="4,2">
                                <ContentPresenter/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="{DynamicResource AccentSoft}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 侧栏导航项 (去编号, 图标 + 标签, 选中=强调药丸; 高亮按 Tag==ActivePage) -->
            <Style x:Key="NavItem" TargetType="Button">
                <Setter Property="Foreground" Value="{DynamicResource Muted}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Margin" Value="0,2"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" Background="Transparent" CornerRadius="9" Padding="12,10">
                                <ContentPresenter VerticalAlignment="Center"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="{DynamicResource Hover}"/>
                                </Trigger>
                                <DataTrigger Value="True">
                                    <DataTrigger.Binding>
                                        <MultiBinding Converter="{StaticResource Eq}">
                                            <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="Tag"/>
                                            <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="DataContext.ActivePage"/>
                                        </MultiBinding>
                                    </DataTrigger.Binding>
                                    <Setter TargetName="b" Property="Background" Value="{DynamicResource AccentSoft}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource Accent}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </DataTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 结果内"视图切换"分段控件项 (横向, 选中=白底浮起) -->
            <!-- 视图切换项 = 原生 pivot tab: 扁平, 选中态 = 深色文字 + 强调色底部指示条 (非浮起卡片) -->
            <Style x:Key="SegItem" TargetType="Button">
                <Setter Property="Foreground" Value="{DynamicResource Muted}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FontSize" Value="13.5"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Grid Background="{TemplateBinding Background}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="3"/>
                                </Grid.RowDefinitions>
                                <ContentPresenter Grid.Row="0" Margin="1,10,1,9" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"/>
                                <Border x:Name="ind" Grid.Row="1" Height="3" Background="Transparent" CornerRadius="1.5"/>
                            </Grid>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
                                    <Setter TargetName="ind" Property="Background" Value="{DynamicResource Line}"/>
                                </Trigger>
                                <!-- 选中: 深色文字(不抢眼的蓝) + 强调色指示条; 用 Medium→SemiBold 极小字重变化避免跳动 -->
                                <DataTrigger Value="True">
                                    <DataTrigger.Binding>
                                        <MultiBinding Converter="{StaticResource Eq}">
                                            <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="Tag"/>
                                            <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="DataContext.ActivePage"/>
                                        </MultiBinding>
                                    </DataTrigger.Binding>
                                    <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter TargetName="ind" Property="Background" Value="{DynamicResource Accent}"/>
                                </DataTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 窗口按钮 (最小化/最大化/关闭) -->
            <Style x:Key="WinBtn" TargetType="Button">
                <Setter Property="Width" Value="46"/>
                <Setter Property="Height" Value="32"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource Muted}"/>
                <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="6">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="{DynamicResource Hover}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="WinClose" TargetType="Button">
                <Setter Property="Width" Value="46"/>
                <Setter Property="Height" Value="32"/>
                <Setter Property="Foreground" Value="{DynamicResource Muted}"/>
                <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" Background="Transparent" CornerRadius="6">
                                <ContentPresenter x:Name="c" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="#E5484D"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 描边次级按钮 (比 Ghost 更显眼: 实描边 + 深色加粗文字)。用于"返回"等需要被看见的次级操作。 -->
            <Style x:Key="Outline" TargetType="Button">
                <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="13.5"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" Background="{DynamicResource Card}" CornerRadius="8"
                                    BorderBrush="{DynamicResource Line}" BorderThickness="1.4" Padding="16,9">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="{DynamicResource AccentSoft}"/>
                                    <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource Accent}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 危险主按钮 (移入回收站等破坏性确认; 红底白字, 与 Primary 同构) -->
            <Style x:Key="Danger" TargetType="Button">
                <Setter Property="Background" Value="{DynamicResource Critical}"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="16,9"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="8"
                                    Padding="{TemplateBinding Padding}" RenderTransformOrigin="0.5,0.5">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="#A81C1A"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="#8E1714"/>
                                    <Setter TargetName="b" Property="RenderTransform">
                                        <Setter.Value><ScaleTransform ScaleX="0.98" ScaleY="0.98"/></Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="b" Property="Background" Value="#D79B9A"/>
                                    <Setter TargetName="b" Property="Opacity" Value="0.7"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 手绘返回箭头 (问题#3: 线性矢量, 非 emoji; 圆角端点, 看着是刻意设计而非随手贴的字符) -->
            <!-- 一根水平轴 + 左端楔形箭头, 描边渲染 (见各处 Path Stroke/StrokeThickness)。 -->
            <Geometry x:Key="IconBackArrow">M11,3 L4,10 L11,17 M4,10 L19,10</Geometry>

            <!-- ===== 自绘矢量图标集 (A: 取代 emoji, 统一线性/实心风格, 看着是刻意设计) ===== -->
            <!-- 描边类 (线性): 用 Stroke 渲染 -->
            <Geometry x:Key="IconSearch">M7.5,2 A5.5,5.5 0 1,0 7.51,2 M11.6,11.6 L16,16</Geometry>
            <Geometry x:Key="IconTrash">M3.5,4.6 H14.5 M6.5,4.6 V3.1 Q6.5,2.3 7.3,2.3 H10.7 Q11.5,2.3 11.5,3.1 V4.6 M5,4.6 L5.8,15.4 Q5.85,16.2 6.6,16.2 H11.4 Q12.15,16.2 12.2,15.4 L13,4.6 M7.7,7.2 V13.4 M10.3,7.2 V13.4</Geometry>
            <Geometry x:Key="IconShield">M9,1.8 L15,4 V8.8 C15,13 12.4,15.2 9,16.4 C5.6,15.2 3,13 3,8.8 V4 Z</Geometry>
            <Geometry x:Key="IconMonitor">M2.5,3 H15.5 V11.5 H2.5 Z M6.5,14.6 H11.5 M9,11.5 V14.6</Geometry>
            <Geometry x:Key="IconPlan">M5,3 H13 V16 H5 Z M7.5,3 V2 H10.5 V3 M7,9 L8.2,10.2 L11,7.2 M7,13 H11</Geometry>
            <Geometry x:Key="IconLock">M4.6,8 H13.4 V16 H4.6 Z M6.3,8 V5.6 C6.3,3.4 7.5,2.5 9,2.5 C10.5,2.5 11.7,3.4 11.7,5.6 V8 M9,11 V13.2</Geometry>
            <Geometry x:Key="IconTune">M3,5 H7 M11,5 H15 M9,3.4 A1.6,1.6 0 1,0 9.01,3.4 M3,13 H7 M11,13 H15 M9,11.4 A1.6,1.6 0 1,0 9.01,11.4</Geometry>
            <Geometry x:Key="IconDoc">M5,2.2 H11 L14,5.2 V16 H5 Z M11,2.2 V5.2 H14 M7,9 H12 M7,12 H12 M7,15 H10</Geometry>
            <Geometry x:Key="IconWarn">M9,2.8 L16,15.2 H2 Z M9,7 V11.1 M9,12.7 V13.3</Geometry>
            <Geometry x:Key="IconCheck">M3,9.5 L6.8,13.3 L15,5</Geometry>
            <Geometry x:Key="IconRestore">M3.6,9 A5.4,5.4 0 1,1 5.2,12.8 M3.6,9 L3,6 M3.6,9 L6.6,8</Geometry>
            <!-- 主题切换: 太阳 (浅色时点击→深色) / 月亮 (深色时点击→浅色) -->
            <Geometry x:Key="IconSun">M9,3.4 A4.2,4.2 0 1,0 9.01,3.4 M9,0.6 V2 M9,16 V17.4 M0.6,9 H2 M16,9 H17.4 M3,3 L4,4 M14,14 L15,15 M15,3 L14,4 M4,14 L3,15</Geometry>
            <Geometry x:Key="IconMoon">M14.5,11.2 A6.2,6.2 0 1,1 7.2,3.2 A4.8,4.8 0 0,0 14.5,11.2 Z</Geometry>
            <!-- 实心类: 用 Fill 渲染 -->
            <Geometry x:Key="IconAi">M9,1.5 L10.7,6.8 L16,8.5 L10.7,10.2 L9,15.5 L7.3,10.2 L2,8.5 L7.3,6.8 Z</Geometry>
            <Geometry x:Key="IconPlay">M5.5,3.5 L14.5,9 L5.5,14.5 Z</Geometry>

            <!-- 图标渲染样式: 描边 / 实心 (统一尺寸/线宽/端点圆角) -->
            <Style x:Key="IconStroke" TargetType="Path">
                <Setter Property="StrokeThickness" Value="1.5"/>
                <Setter Property="StrokeStartLineCap" Value="Round"/>
                <Setter Property="StrokeEndLineCap" Value="Round"/>
                <Setter Property="StrokeLineJoin" Value="Round"/>
                <Setter Property="Stretch" Value="Uniform"/>
                <Setter Property="Width" Value="16"/>
                <Setter Property="Height" Value="16"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
                <Setter Property="SnapsToDevicePixels" Value="True"/>
            </Style>
            <Style x:Key="IconFill" TargetType="Path">
                <Setter Property="Stretch" Value="Uniform"/>
                <Setter Property="Width" Value="16"/>
                <Setter Property="Height" Value="16"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>

            <!-- 列表行按钮 (扁平; 悬停=Hover, 按下=AccentSoft; 取代原生 Button 的亮蓝 Aero 高亮) -->
            <Style x:Key="RowButton" TargetType="Button">
                <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <!-- 静止态边框透明 (消除每行的"黑边"); hover 才点亮蓝边, 厚度恒为 1 → 不抖动 -->
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="Transparent"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" Background="Transparent" CornerRadius="8" Padding="{TemplateBinding Padding}"
                                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="{DynamicResource Hover}"/>
                                    <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="{DynamicResource AccentSoft}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 折叠分组 (按软件页): 干净的可点表头 + 旋转 chevron, 无虚线焦点框、无默认黑边/色块 -->
            <Style x:Key="CardExpander" TargetType="Expander">
                <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Expander">
                            <StackPanel>
                                <ToggleButton x:Name="Header" Cursor="Hand" Focusable="False"
                                              FocusVisualStyle="{x:Null}" HorizontalContentAlignment="Stretch"
                                              IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border x:Name="hb" Background="Transparent" CornerRadius="8" Padding="8,7"
                                                    BorderBrush="Transparent" BorderThickness="1">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <Path x:Name="chev" Grid.Column="0" Width="11" Height="11" Margin="2,0,10,0"
                                                          Stretch="Uniform" Stroke="{DynamicResource Muted}" StrokeThickness="2"
                                                          StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round"
                                                          Data="M 0 0 L 5 5 L 10 0" VerticalAlignment="Center"
                                                          RenderTransformOrigin="0.5,0.5">
                                                        <Path.RenderTransform><RotateTransform Angle="-90"/></Path.RenderTransform>
                                                    </Path>
                                                    <ContentPresenter Grid.Column="1" VerticalAlignment="Center"/>
                                                </Grid>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="hb" Property="Background" Value="{DynamicResource Hover}"/>
                                                    <Setter TargetName="hb" Property="BorderBrush" Value="{DynamicResource Line}"/>
                                                </Trigger>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="chev" Property="RenderTransform">
                                                        <Setter.Value><RotateTransform Angle="0"/></Setter.Value>
                                                    </Setter>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                    <ContentPresenter ContentSource="Header"/>
                                </ToggleButton>
                                <ContentPresenter x:Name="content" Visibility="Collapsed"/>
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsExpanded" Value="True">
                                    <Setter TargetName="content" Property="Visibility" Value="Visible"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 标题栏 AI 状态胶囊 (带标签可点; hover 微亮) -->
            <Style x:Key="AiPill" TargetType="Button">
                <Setter Property="Foreground" Value="{DynamicResource Muted}"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="b" Background="{DynamicResource Subtle}" CornerRadius="8"
                                    BorderBrush="{DynamicResource Line}" BorderThickness="1" Padding="11,5">
                                <ContentPresenter VerticalAlignment="Center"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="{DynamicResource AccentSoft}"/>
                                    <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource Accent}"/>
                                    <Setter Property="Foreground" Value="{DynamicResource Ink}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
