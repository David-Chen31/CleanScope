# CleanScope 安全测试门禁 (T-01 ~ T-20)

> 已确认 (2026-06-14)：安全测试 T-01～T-20 纳入 **CI 强制构建门禁**，任一失败即阻断合并/发布。
> 节奏：**边写边测** —— 每条用例随对应模块落地，不积压到最后。本表是 T4.5 的汇总核对。
> 门禁载体：[.github/workflows/ci.yml](.github/workflows/ci.yml)（windows-latest，`dotnet test CleanScope.sln`）。

## 红线拦截类

| 编号 | 用例 | 落地测试 | 状态 |
|---|---|---|---|
| T-01 | MVP 任意删除意图 → Rejected | `SafetyGuardMvpTests` | ✅ |
| T-02 | System32 删除 → Rejected + 黑名单 | `SafetyGuardAdmissionTests.System32_*` | ✅ |
| T-03 | Installer\*.msi → Rejected + 官方替代 | `SafetyGuardAdmissionTests.Installer_*` | ✅ |
| T-04 | 风险 D/E 删除 → Rejected | `SafetyGuardAdmissionTests.High_risk_*` | ✅ |
| T-05 | 被占用文件删除 → Rejected | `SafetyGuardAdmissionTests.Occupied_*` | ✅ |
| T-06 | symlink→System32 解析后 Rejected | `SafetyGuardAdmissionTests.Symlink_*` | ✅ |
| T-07 | 无永久删除 API 代码路径 | `NoPermanentDeleteTests` (静态扫描 src) | ✅ |
| T-08 | 无一键批量删除 | `NoPermanentDeleteTests` (DeleteAll/Many/Purge 令牌) | ✅ |

## AI 安全类

| 编号 | 用例 | 落地测试 | 状态 |
|---|---|---|---|
| T-09 | AI 对黑名单返回可删 → 拦截改判 | `AiOutputValidatorTests.Blacklist_*` | ✅ |
| T-10 | AI 风险低于引擎 → 以引擎为准 | `AiOutputValidatorTests.Ai_risk_below_engine_*` | ✅ |
| T-11 | AI 缺证据/置信度 → 非法不展示 | `AiOutputValidatorTests.Missing_*` | ✅ |
| T-12 | 证据不足却确定 → 降级 E | `AiOutputValidatorTests.Insufficient_*` | ✅ |
| T-13 | AI 输出含命令文本 → 仅作文本 | `AiOutputValidatorTests.Command_like_*` | ✅ |
| T-14 | AI 不可用/超时 → 规则解释降级 | `ExplanationServiceTests.Cloud_failure_*` | ✅ |

## 隐私类

| 编号 | 用例 | 落地测试 | 状态 |
|---|---|---|---|
| T-15 | 出云载荷脱敏(无内容/用户名/文档名) | `SanitizationGatewayTests.Payload_strips_*` | ✅ |
| T-16 | 关闭云端 → 全程本地无外发 | `ExplanationServiceTests.Disabled_cloud_never_invokes_remote` | ✅ |
| T-17 | 上云仅最小必要字段 | `SanitizationGatewayTests.Carries_only_allowed_*` | ✅ |

## 鲁棒性类 (fail-safe)

| 编号 | 用例 | 落地测试 | 状态 |
|---|---|---|---|
| T-18 | 风险判定异常 → "不建议删除" 不崩 | `RiskEngineTests.Exception_*_fails_safe_to_E` | ✅ |
| T-19 | 无权限目录 → 记录而非猜测 | `ScanEngineRobustnessTests.DeniedState_*` | ✅ |
| T-20 | 删除前写审计失败 → 中止操作 | `AuditBeforeExecuteTests.Audit_write_failure_aborts_*` | ✅ |

---

**架构红线 (同为门禁)**：`DependencyRulesTests`（Ai 不依赖 Safety＝决议10、Core/Domain 不依赖 WPF、SQLite 仅在 Infrastructure、Domain 最内层）。

**结论**：T-01~T-20 全部随对应模块落地且通过；CI 门禁 (`.github/workflows/ci.yml`) 一旦推送到 GitHub 即生效，任一安全用例失败阻断构建。
